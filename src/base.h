#ifndef BASE_H
#define BASE_H

#define SUCCESS true
#define FAILURE false

// data-type representing a pointer to a double -> double function
typedef double (*func)(double*);
typedef double (*realfunc)(double);

#endif
